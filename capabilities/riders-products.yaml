naftiko: 1.0.0-alpha2
info:
  label: Uber Riders API — Products
  description: 'Rides — Products. 2 operations. Lead operation: List Products. Self-contained Naftiko capability covering
    one Uber business surface.'
  tags:
  - Uber
  - Rides
  - Products
  created: '2026-06-03'
  modified: '2026-06-03'
binds:
- namespace: env
  keys:
    UBER_API_TOKEN: UBER_API_TOKEN
capability:
  consumes:
  - type: http
    namespace: riders-products
    baseUri: https://api.uber.com/v1.2
    description: Uber Riders API — Products business capability. Self-contained, no shared references.
    authentication:
      type: bearer
      token: '{{env.UBER_API_TOKEN}}'
    resources:
    - name: products
      path: /products
      operations:
      - name: listProducts
        method: GET
        description: List Products
        inputParameters:
        - name: latitude
          in: query
          type: number
          required: true
          description: Latitude component of location.
        - name: longitude
          in: query
          type: number
          required: true
          description: Longitude component of location.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: products-product-id
      path: /products/{product_id}
      operations:
      - name: getProduct
        method: GET
        description: Get Product
        inputParameters:
        - name: product_id
          in: path
          type: string
          required: true
          description: Unique identifier representing a specific product for a given location.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: riders-products-rest
    port: 8080
    description: REST adapter for Uber Riders API — Products. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/products
      name: products
      description: REST surface for products.
      operations:
      - method: GET
        name: listProducts
        description: List Products
        call: riders-products.listProducts
        with:
          latitude: rest.latitude
          longitude: rest.longitude
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/products/{product-id}
      name: products-product-id
      description: REST surface for products-product-id.
      operations:
      - method: GET
        name: getProduct
        description: Get Product
        call: riders-products.getProduct
        with:
          product_id: rest.product_id
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: riders-products-mcp
    port: 9090
    transport: http
    description: MCP adapter for Uber Riders API — Products. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: list-products
      description: List Products
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: riders-products.listProducts
      with:
        latitude: tools.latitude
        longitude: tools.longitude
      outputParameters:
      - type: object
        mapping: $.
    - name: get-product
      description: Get Product
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: riders-products.getProduct
      with:
        product_id: tools.product_id
      outputParameters:
      - type: object
        mapping: $.
