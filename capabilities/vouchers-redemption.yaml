naftiko: 1.0.0-alpha2
info:
  label: Uber Vouchers API — Redemption
  description: 'Uber Vouchers API — Redemption. 1 operations. Lead operation: Redeem Voucher Code. Self-contained Naftiko
    capability covering one Uber business surface.'
  tags:
  - Uber
  - Redemption
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    UBER_API_KEY: UBER_API_KEY
capability:
  consumes:
  - type: http
    namespace: vouchers-redemption
    baseUri: https://api.uber.com/v1
    description: Uber Vouchers API — Redemption business capability. Self-contained, no shared references.
    resources:
    - name: me-vouchers-redeem
      path: /me/vouchers/redeem
      operations:
      - name: redeemvouchercode
        method: POST
        description: Redeem Voucher Code
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.UBER_API_KEY}}'
  exposes:
  - type: rest
    namespace: vouchers-redemption-rest
    port: 8080
    description: REST adapter for Uber Vouchers API — Redemption. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/me/vouchers/redeem
      name: me-vouchers-redeem
      description: REST surface for me-vouchers-redeem.
      operations:
      - method: POST
        name: redeemvouchercode
        description: Redeem Voucher Code
        call: vouchers-redemption.redeemvouchercode
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: vouchers-redemption-mcp
    port: 9090
    transport: http
    description: MCP adapter for Uber Vouchers API — Redemption. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: redeem-voucher-code
      description: Redeem Voucher Code
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: vouchers-redemption.redeemvouchercode
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
